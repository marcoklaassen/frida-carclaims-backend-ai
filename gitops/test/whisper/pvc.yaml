apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisper-model
  namespace: frida-carclaims-test
  labels:
    app: whisper
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
