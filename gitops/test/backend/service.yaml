apiVersion: v1
kind: Service
metadata:
  name: voice-backend
  namespace: frida-carclaims-test
  labels:
    app: voice-backend
spec:
  selector:
    app: voice-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
